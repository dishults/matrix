import Models

//let u = Vector([2.0, 3.0])
//let v = Vector([5.0, 7.0])
////u.add(v)
//print("\(u)")

let u = Matrix([
  [1.0, 2.0],
  [3.0, 4.0],
])
let v = Matrix([
  [7.0, 4.0],
  [-2.0, 2.0],
])
//u.add(v)
print("\(u)")
